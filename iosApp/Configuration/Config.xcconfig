TEAM_ID=

BUNDLE_ID_PREFIX=com.example

APP_BUNDLE_IDENTIFIER=$(BUNDLE_ID_PREFIX).opencodepocket
SHARE_EXTENSION_BUNDLE_IDENTIFIER=$(BUNDLE_ID_PREFIX).opencodepocket.shareextension
TESTS_BUNDLE_IDENTIFIER=$(BUNDLE_ID_PREFIX).opencodepocket.tests

APP_GROUP_IDENTIFIER=group.$(BUNDLE_ID_PREFIX).opencodepocket

PRODUCT_NAME=OpenCodePocket
PRODUCT_BUNDLE_IDENTIFIER=$(APP_BUNDLE_IDENTIFIER)

CURRENT_PROJECT_VERSION=1
MARKETING_VERSION=1.0
